<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /tsukuyomi/public/

    # Redireciona tudo que não é um arquivo ou diretório existente para index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php?/$1 [L]
</IfModule>